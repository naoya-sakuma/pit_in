<div class="container-fluid weekly_plan_index">
  <h4 class="monthly_plan_page_title">今月の計画</h4>
  <small class="monthly_plan_guide">現在取組中の目標とその解決案を表示しています。編集ボタンから今月取り組む解決案を選択してください。</small>

  <% @goals.each do |goal| %>
    <div class="card monthly_plan_card">
      <div class="row">
        <strong class="card-title goal_title_in_monthly col-8"><%= goal.title %></strong>
        <div class="col-8"><%= link_to '編集', edit_weekly_plan_path(goal) %></div>
      </div>

      <ul class="list-group list-group-flush">
        <% goal.problems.each do |problem| %>
          <% problem.solutions.each do |solution| %>
            <li class="list-group-item solution_title_in_monthly"><%= solution.title %></li>
        <% solution.tasks.each do |task|%>
          <p><%= task.title %></p>
        <% end %>
      </div>
    <% end %>
  <% end %>
<% end %>
